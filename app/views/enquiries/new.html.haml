= provide :edit, link_to('View Enquiries', [:admin, :enquiries])

#banner{style: "background-image: url(#{@page.banner})"}
#content_wrapper
  #content
    .split_page
      .main_column.body_text
        = raw @page.body

      .side_column
        .arrangement_form
          %h3 Arrange a callback now
          = form_for @enquiry do |f|
            = render 'fields', f: f